info:
  name: delete_customer
  type: http
  seq: 4

http:
  method: DELETE
  url: "{{baseURL}}/customers/:id"
  params:
    - name: id
      value: "13"
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
