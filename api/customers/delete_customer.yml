info:
  name: delete_customer
  type: http
  seq: 3

http:
  method: DELETE
  url: "{{baseURL}}/customers/:id"
  params:
    - name: id
      value: ""
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
