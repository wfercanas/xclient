info:
  name: create_customer
  type: http
  seq: 1

http:
  method: POST
  url: "{{baseURL}}/customers"
  body:
    type: json
    data: |-
      {
        "tenant_id": 7,
        "customer_type": "person",
        "customer_id": "12340001",
        "name": "John Doe",
        "status": "active",
        "association_date": "2020-08-01"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
