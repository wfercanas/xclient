info:
  name: get_customer_by_id
  type: http
  seq: 2

http:
  method: GET
  url: "{{baseURL}}/customers/:id"
  params:
    - name: id
      value: "13"
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
