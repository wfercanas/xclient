info:
  name: Contribution By Id
  type: http
  seq: 2

http:
  method: GET
  url: "{{baseURL}}/contributions/:id"
  params:
    - name: id
      value: 1-10001851221
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
