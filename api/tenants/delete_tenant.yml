info:
  name: delete_tenant
  type: http
  seq: 2

http:
  method: DELETE
  url: "{{baseURL}}/tenants/{id}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
