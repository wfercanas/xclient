info:
  name: delete_tenant
  type: http
  seq: 4

http:
  method: DELETE
  url: "{{baseURL}}/tenants/:id"
  params:
    - name: id
      value: "5"
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
