info:
  name: get_tenants
  type: http
  seq: 2

http:
  method: GET
  url: "{{baseURL}}/tenants"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
