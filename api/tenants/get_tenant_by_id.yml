info:
  name: get_tenant_by_id
  type: http
  seq: 3

http:
  method: GET
  url: "{{baseURL}}/tenants/:id"
  params:
    - name: id
      value: "7"
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
