info:
  name: create_tenant
  type: http
  seq: 3

http:
  method: POST
  url: "{{baseURL}}/tenants"
  body:
    type: json
    data: |-
      {
        "name": "febanc"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: true
