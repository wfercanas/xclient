info:
  name: create_tenant
  type: http
  seq: 1

http:
  method: POST
  url: "{{baseURL}}/tenants"
  body:
    type: json
    data: |-
      {
        "name": "acme"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
